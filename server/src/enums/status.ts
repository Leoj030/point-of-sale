enum Status {
    ACTIVE = 'active',
    INACTIVE = 'inactive',
}

export default Status;

export enum OrderStatus {
    Pending = 'Pending',
    Completed = 'Completed',
    Cancelled = 'Cancelled',
}

export enum OrderType {
    DineIn = 'Dine In',
    TakeOut = 'TakeOut',
    Delivery = 'Delivery',
}

export enum PaymentMethod {
    Cash = 'Cash',
}
