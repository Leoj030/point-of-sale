enum Status {
    ACTIVE = 'active',
    INACTIVE = 'inactive',
}

export default Status;

export enum OrderType {
    DineIn = 'Dine In',
    TakeOut = 'Take Out',
    Delivery = 'Delivery',
}

export enum PaymentMethod {
    Cash = 'Cash',
    GCash = 'GCash',
}
